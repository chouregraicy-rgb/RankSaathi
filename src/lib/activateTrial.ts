/**
 * activateTrial.ts — DISABLED
 * Free trial removed. All users must purchase ₹499 lifetime plan.
 */
export async function activateTrial(_accessToken: string): Promise<void> {
  // Trial disabled — do nothing
  return;
}
